runtime: nodejs18
env: standard

handlers:
  - url: /.*
    secure: always
    redirect_http_response_code: 301
    script: auto

env_variables:
  NODE_ENV: "production"

build:
  commands:
    - npm install --legacy-peer-deps
    - npm run build 